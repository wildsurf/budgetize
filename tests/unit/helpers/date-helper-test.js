import {
  dateHelper
} from 'budgetize/helpers/date-helper';

module('DateHelperHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = dateHelper(42);
  ok(result);
});
